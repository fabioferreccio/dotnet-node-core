/**
 * Options for configuring AnsiConsole behavior.
 * @experimental This API is in preview and may change.
 */
export interface AnsiConsoleOptions {
    /**
     * The width of the console in characters.
     * Defaults to 80 for deterministic rendering.
     */
    width?: number;
}
